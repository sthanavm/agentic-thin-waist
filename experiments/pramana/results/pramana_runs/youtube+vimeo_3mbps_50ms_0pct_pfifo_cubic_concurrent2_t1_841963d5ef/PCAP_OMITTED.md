# Capture omitted from this copy

This run is a reference example shipped in the repository. Its `record.json`
says `pcap_saved: true` because the run did save a capture on the VM that
produced it — captures are 30-140 MB each and do not belong in a git repo.

The record, plots, collector log and raw per-second samples here are the
unmodified originals. Cross-checking against the capture was done on the VM
before this run was accepted.
